#!/usr/bin/node

const arg = process.argv;
if (arg.length === 4) {
  console.log(arg[2], 'is', arg[3]);
} else if (arg.length === 3) {
  console.log(arg[2], 'is', arg[3]);
} else if (arg.length === 2) {
  console.log(arg[2], 'is', arg[3]);
}
